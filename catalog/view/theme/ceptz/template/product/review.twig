{% if reviews %}
{% for review in reviews %}
  <div class="review-list">
    <div class="author"><b>{{ review.author }}</b>
      <br/><div class="rating"><i class="fa fa-star      {% if review.rating >= 1 %} active {% endif %}"></i>
        <i class="fa fa-star{% if review.rating >= 2 %} active {% endif %}"></i>
        <i class="fa fa-star{% if review.rating >= 3 %} active {% endif %}"></i>
        <i class="fa fa-star {% if review.rating >= 4 %} active {% endif %}"></i>
        <i class="fa fa-star {% if review.rating >= 5 %} active {% endif %}"></i>
      </div>
    </div>
    <div class="pull-right"><span>{{ review.date_added }}</span></div>
    <div class="text">{{ review.text }}</div>
  </div>
  <?php } ?>
{% endfor %}
  <div class="row pagination-results">
    <div class="col-sm-6 text-left">{{ pagination }}</div>
    <div class="col-sm-6 text-right">{{ results }}</div>
  </div>

<div class="text-right">{{ pagination }}</div>
{% else %}
<p>{{ text_no_reviews }}</p>
{% endif %}

